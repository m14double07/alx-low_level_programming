#include <stdio.h>

/**
*main - Entry point on the program
*
*Return: 0 at success
*/

int main(void)

{
	char a, e, q;

	e = 'e';
	q = 'q';

	for (a = 'a'; a <= 'z'; a++)

	{
	for
		(a != e && a != q)

		putchar(a); }


	putchar('\n');

	return (0);

}
